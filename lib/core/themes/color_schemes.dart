import 'package:flutter/material.dart';

/// Warna utama aplikasi
class AppColors {
  // === Brand Colors ===
  static const Color primary = Color(0xFFF58C3C);
  static const Color secondary = Color(0xFFE4E4E4);

  // === Button Colors ===
  static const Color secondaryButton = Color(0xFFD8742E);

  // === State Colors ===
  static const Color success = Color(0xFF00FF0A);
  static const Color danger = Color(0xFFFF0000);

  // === Text Colors (Opsional) ===
  static const Color textPrimary = Colors.black;
  static const Color textOnPrimary = Colors.white;

  // === Background Colors (Opsional) ===
  static const Color background = Colors.white;
  static const Color scaffoldBackground = Color(0xFFF9F9F9);
}
